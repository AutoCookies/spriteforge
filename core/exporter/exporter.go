package exporter

import (
	"pixelc/core/compiler"
	"pixelc/pkg/model"
)

func ExportUnity(atlas model.Atlas, atlasImageName string, version string) ([]byte, error) {
	return nil, compiler.ErrNotImplemented
}
